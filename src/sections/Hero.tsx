const Hero = () => (
  <section>
    <p>
      <b>Explore the data.</b>
    </p>
    <p>
      This website lets you sort, filter, combine, and visualize over{" "}
      {(170000).toLocaleString()} samples of publicly available 16S rRNA
      amplicon sequencing data, all processed using the same pipeline and
      reference database.
    </p>
  </section>
);

export default Hero;
