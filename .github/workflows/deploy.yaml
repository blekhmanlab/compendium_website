name: Deploy web app

on:
  push:
    branches:
      - main

jobs:
  deploy-web-app:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install packages
        run: yarn install

      - name: Build app
        run: yarn build
